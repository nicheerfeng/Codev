# Terax 二开规划文档(交接版)

> **状态:进行中,已交接。** 本文档记录:原始需求、已完成的改造、当前工作区状态、剩余待办、关键决策与风险。新接手开发者请先通读本节,再按「六、剩余工作」继续。
>
> 最后更新:2025-08 中断时刻。**工作区存在已验证但未提交的改动(见「四、未提交改动」),接手后请先提交。**

---

## 一、原始需求(用户确认版)

1. **多文件夹导入**:保留文件树,支持多个文件夹(类似 VS Code 多根工作区),当前单窗口仅支持单文件夹。
2. **核心功能集**:项目文件树 + 文件阅览 + 多终端 + 中英文切换。
3. **模块裁剪**:去除 source-control / git-history / agents / ai(含 model 配置)/ control / updater 等与核心需求无关的模块。
4. **一键唤醒终端**:右键点击任意文件或文件夹 → 在右侧终端面板打开对应路径终端(文件取父目录)。
5. **终端面板布局**:终端从顶部 tab 体系剥离,独立停靠在文件阅览器右侧,可水平放置多个终端(不抢占顶部文件名空间)。
6. **去除 vim 模式**。
7. **修复 D 盘/盘符导航 bug**(无法选择 D 盘、配置入口难找)。
8. **中英文切换**(i18n,仅核心 UI 优先)。

---

## 二、已完成的改造(按提交记录)

### Step 0 — git 基线 ✅

- `git init` + 基线提交 `703141a baseline: terax v0.8.6 upstream snapshot before 2nd-dev`。

### 编译优化 ✅(用户提出后已实施)

- 新增 `src-tauri/.cargo/config.toml`:`jobs = 6` + `[profile.dev] debug = false`。
  - 原因:20 核并行 rustc 每个进程吃 1-3GB,16GB 内存机器被打爆卡死。
  - release 编译不受影响。

### Step 1 — 多根文件树 + 盘符导航 ✅

**Rust 侧**(已提交 step1):

- `src-tauri/src/modules/fs/tree.rs`:新增 `fs_list_drives` 命令(Windows 枚举 A:-Z: 存在的盘符,返回 `C:/` 形态;其他平台返回 `["/"]`)。
- `src-tauri/src/modules/workspace.rs`:`resolve_path`(Windows 分支)兼容裸盘符 `C:` → `C:/`(修复 `read_dir("C:")` 语义错误,这是"没法选 D 盘"的根因之一)。
- `src-tauri/src/lib.rs`:注册 `fs_list_drives`。

**前端侧**:

- `src/modules/settings/store.ts`:新增 `workspaceRoots: string[]`、`activeWorkspaceRoot: string | null` 偏好字段 + `normalizeWorkspaceRoot(s)` 工具 + `setWorkspaceRoots`/`setActiveWorkspaceRoot` setter(持久化到 tauri-plugin-store,沿用 `KEY_*` + `writePref` + `PREFERENCE_KEYS` 模式)。
- 新增 `src/modules/tabs/lib/useWorkspaceRoots.ts`:roots 增删/激活 hook(去重、规范化、越界回退)。
- `src/modules/tabs/lib/useWorkspaceCwd.ts`:增加第 4 参 `rootOverride`(有激活根时优先)。
- `src/modules/explorer/RootTree.tsx`:**从 FileExplorer.tsx 拆分出的单根树组件**(原逻辑迁移,新增 `onAddAsRoot` prop;右键菜单"Open in Terminal"对文件也生效,取父目录;新增"Add to Workspace Roots")。
- `src/modules/explorer/FileExplorer.tsx`:**重写为多根容器**:
  - 顶部工具条(Workspace 标题 + 添加根按钮 + 路径输入框);
  - **盘符条**(Windows,`fs_list_drives`,点击进入浏览模式);
  - 浏览模式(返回按钮 + RootTree,可下钻,右键可"添加为工作区根");
  - 根集合模式(每个根一个 `RootSection`:折叠/展开、激活高亮、移除按钮 + RootTree)。
- `src/app/App.tsx`:`useWorkspaceRoots` 接线,`useWorkspaceCwd` 传 rootOverride = `activeRoot ?? roots[0] ?? null`,FileExplorer 传新 props。

### Step 2 — 终端面板布局重构 + 一键唤醒终端 ✅

- 新增 `src/modules/terminal/TerminalPanel.tsx`:右侧停靠面板(顶部小标签条:终端 tab 横向排列、关闭、新建、折叠按钮 + 复用 `TerminalStack`)。
- `src/app/App.tsx`:
  - 派生 `fileTabs` / `terminalTabs` / `fileActiveId` / `terminalActiveId`(各自维护最近激活 id,切换互不干扰);
  - 布局:workspace 面板内嵌套 `ResizablePanelGroup`(主区域 | 终端面板),面板 `collapsible`,有终端自动展开、无终端自动折叠(`panelRef` + `PanelImperativeHandle`,注意 react-resizable-panels v4 用 `panelRef` prop 而非 `ref`,Group 用 `orientation` 而非 `direction`);
  - Header/TabBar 只显示文件类 tabs;`WorkspaceSurface` 只收 fileTabs。
- 右键"在此路径打开终端" → `cdInNewTab`(已存在)→ 创建 terminal tab 落在右侧面板并自动展开。
- **冒烟通过**:`pnpm tauri dev` 启动无错误,TerminalPanel 组件正常渲染。

### Step 3a — 裁剪 updater + control/CLI ✅(已提交)

**前端**:

- 删除 `src/modules/updater/`、`src/modules/control/`;
- `src/settings/sections/AboutSection.tsx`:移除 useUpdater 逻辑(保留版本/链接信息);
- `src/app/App.tsx`:移除 UpdaterDialog、useControlBridge、openControlFile。

**Rust/配置**:

- 删除 `src-tauri/src/modules/control.rs`、`crates/terax-control-protocol/`、`crates/terax-cli/`、`scripts/build-cli.mjs`;
- `lib.rs`:移除 control 插件/状态/命令、updater 插件、RunEvent::Exit 的 control shutdown;
- **pty 模块解耦 control**:`pty/mod.rs`(pty_open 去 control 参数)、`pty/session.rs`(spawn 去 control 参数)、`pty/shell_init.rs`(build_command/apply_common 去 ShellControlEnv 注入,测试同步改造);
- `Cargo.toml`:workspace members 清空、移除 terax-control-protocol/tauri-plugin-updater;
- `build.rs`:删除 configure_sidecar(保留 `tauri_build::build()`);
- `tauri.conf.json`:移除 externalBin、createUpdaterArtifacts、plugins.updater、beforeDevCommand/beforeBuildCommand 中的 build:cli;
- `capabilities/desktop.json`:移除 `updater:default`;
- `package.json`:删除 build:cli / build:cli:dev 脚本。

### Step 3b — 裁剪 source-control + git-history(前端)✅(已提交)

- 删除模块目录 `src/modules/source-control/`、`src/modules/git-history/`;
- 删除 editor 的 `AiDiffStack*` / `GitDiffStack*` / `AiDiffPane` / `GitDiffPane`(6 文件)+ index.ts 导出;
- `useTabs.ts`:删除 AiDiff/GitDiff/GitHistory/GitCommitFileDiff tab kinds 类型、planGitDiffOpen/planCommitHistoryOpen、openAiDiffTab/openGitDiffTab/openCommitHistoryTab/openCommitFileDiffTab/setAiDiffStatus/closeAiDiffTab、相关导出;Tab 联合类型缩为 `TerminalTab | EditorTab | PreviewTab | MarkdownTab`;
- `tabLabel.ts`、`TabBar.tsx`、`tabs/index.ts`:git/ai kind 分支清理;
- `WorkspaceSurface.tsx`:移除 GitDiffStack/GitHistoryStack/AiDiffStack 层;
- `App.tsx`:移除 source-control/git-history 全部接线(useSourceControlContext/useRepositoryTargeting/SourceControlPanel/侧边栏视图/git 快捷键/palette 项/activeFilePath git 分支/WorkspaceSurface props);
- `sidebar/types.ts`:`SidebarViewId = "explorer"`;`SidebarRail` 移除 source-control 项与 changedCount;`useSidebarPanel` 同步;
- `command-palette/commands.ts` + test:移除 git/ai 命令项与类型字段;
- `useEditorFileSync.ts`:移除 ai-diff approval reload effect;
- 测试:`planGitDiffOpen.test.ts`、`planCommitHistoryOpen.test.ts` 删除,`serialize.test.ts` git-diff fixture 清理。

### Step 3c — 裁剪 agents + ai(前端,进行中,**未提交**)⏳

**已完成(tsc 全绿)**:

- `App.tsx`:移除全部 ai 引用(useChatStore/useAiBootstrap/useAiLiveBridge/useSelectionAskAi/AiComposerProvider/native/composer 状态与回调/WorkspaceInputBar/AgentRunBridge/LocalAgentNotificationsBridge/AiMiniWindow/SelectionAskAi/ask 与 mini 弹层/AiComposerProvider 根包装);移除 agents 引用(AgentNotificationsBridge/launchAgentGroup/activateAgentTarget/onActivateAgent/acknowledgeAttention effect);补回误删的 `useZoom`/`isMarkdownPath` import;
- `Header.tsx`:移除 NotificationBell(两处)、onLaunchAgents/onActivateAgent/onActivateLocalAgent props;
- `TabBar.tsx`:移除 agent status(TabIcon agent 分支/useTabAgentStatus)、onLaunchAgents;
- `NewTabMenu.tsx`:**整个重写**(移除 Agents 菜单项、AgentLauncherPanel、Popover launcher 逻辑);
- `StatusBar.tsx`:移除 onOpenMini/onOpenAi/hasComposer props 与 AI 状态栏控件(AgentStatusPill/AiOpenButton/AiStatusBarControls/useChatStore/panelOpen);
- `command-palette/commands.ts` + test:移除 ai.toggle/ai.askSelection 命令项与字段。
- `pnpm check-types`(tsc --noEmit)✅ 全绿。

---

## 三、关键架构决策(新接手必读)

1. **多根模型**:roots 存在 **preferences store(全局)**,非 per-space(与原始规划文档中"扩展 spaces"的设想不同,实际实现更简单可控,后续可平滑迁移到 per-space)。
2. **终端面板**:不改 useTabs 的 tab 模型,terminal tab 仍是 tab,只是**视图层拆分**——Header/TabBar 与 WorkspaceSurface 只渲染 fileTabs,TerminalPanel 渲染 terminalTabs。spaces 序列化天然兼容。
3. **react-resizable-panels v4 语法**:Panel 用 `panelRef`(非 ref)+ `PanelImperativeHandle`(collapse/expand/isCollapsed);Group 用 `orientation`(非 direction)。
4. **pnpm 生态**,不用 npm/yarn;路径一律 `@/`。
5. **质量闸门**:`pnpm lint` / `pnpm check-types` / `pnpm test`;Rust:`cargo clippy --all-targets --locked -- -D warnings` + `cargo test --locked`。

---

## 四、未提交改动(接手后先处理)

工作区有 7 个已修改文件(**tsc 已验证全绿**):
`src/app/App.tsx`、`src/modules/command-palette/commands.ts`、`src/modules/command-palette/commands.test.ts`、`src/modules/header/Header.tsx`、`src/modules/statusbar/StatusBar.tsx`、`src/modules/tabs/NewTabMenu.tsx`、`src/modules/tabs/TabBar.tsx`

建议:接手后先 `git add -A && git commit`,再继续剩余工作。

---

## 五、仍引用 ai/agents 的残留文件(删目录前必须处理)

以下文件仍 `import` 自 `@/modules/ai` 或 `@/modules/agents`(tsc 目前全绿是因为它们各自独立,但删除模块目录前必须清理):

| 文件 | 引用内容 | 建议 |
| --- | --- | --- |
| `src/app/hooks/useWorkspaceSwitcher.ts` | ai | 移除相关调用 |
| `src/app/components/WorkspaceInputBar.tsx` | ai | **整个文件删除**(不再被引用) |
| `src/app/components/useGitBranch.ts` | ai | **整个文件删除**(git 已裁) |
| `src/components/ai-elements/chat-code.tsx`、`tool.tsx` | ai | 删除文件(ai-elements 组件) |
| `src/settings/sections/ModelsSection.tsx`、`AgentsSection.tsx` | ai/agents | 删除文件 + SettingsApp 移除 tab |
| `src/settings/components/ProviderKeyCard.tsx`、`ProviderIcon.tsx` | ai | 删除文件 |
| `src/settings/sections/GeneralSection.tsx` | agents | 移除 agents 相关段落 |
| `src/modules/explorer/RootTree.tsx` + `lib/gitStatusUtils.ts`(+test)/`useGitStatus.ts`/`gitStatusColor.ts` | git 装饰 | 移除 git 装饰(explorer 裁剪) |
| `src/modules/editor/lib/autocomplete/`、`EditorPane.tsx`、`lib/diffCache.ts` | ai 补全/ai diff | 移除 AI 补全与 ai-diff 相关 |
| `src/modules/terminal/block/BlockOverlay.tsx` | ai(确认后) | block 模式如依赖 ai 则一并移除 |
| `src/modules/spaces/lib/useSpacesBoot.ts`、`src/modules/settings/store.ts` | ai(model 字段) | settings store 的 model/AI 字段按需清理 |
| `src/modules/statusbar/StatusBar.tsx`(已改) | — | 确认无残留 |

---

## 六、剩余工作(按依赖顺序)

### Phase A — 完成 ai/agents 裁剪(前端收尾)

1. 提交当前未提交改动(见「四」)。
2. 按「五」表格清理残留文件(含删除 `src/modules/ai/`、`src/modules/agents/`、`src/components/ai-elements/` 目录,清理 editor autocomplete/diffCache、terminal block 依赖、explorer git 装饰、settings models/agents 相关)。
3. `pnpm check-types` + `pnpm test` 全绿;删除目录后跑 `pnpm lint`。
4. 提交。

### Phase B — Rust 侧裁剪

1. 删除 `src-tauri/src/modules/git/`(8 文件)、`agent.rs`、`net.rs`、`secrets.rs`(按用户决策 ai 全删,secrets 一并删);
2. `lib.rs` 移除对应命令注册(git_*、agent_*、ai_http_*、lm_ping、secrets_*)与 `use` 语句;
3. `pty/agent_detect.rs` / `pty/` 中的 AGENT_EVENT、agent 检测代码移除(前端 agentActivity 已删);
4. `Cargo.toml`:移除 reqwest/bytes/futures-util/keyring/grep-* 等不再使用的依赖(`cargo machete` 或手动核对;`ignore`、`globset`、`nucleo-matcher` 可能仍被 fs search 使用,先确认再删);
5. `cargo check` + `cargo clippy` 全绿;提交。

### Phase C — 去除 vim

1. `src/modules/settings/store.ts`:移除 `vimMode` 字段/`KEY_VIM_MODE`/默认值/loadPreferences/映射(5 处);
2. `src/modules/editor/lib/vim.ts` 删除、`extensions.ts`/`EditorPane.tsx` 去 vim 集成、快捷键清理;
3. `package.json` 移除 `@replit/codemirror-vim` 依赖;tsc/test 全绿;提交。

### Phase D — 中英文切换(i18n,核心 UI)

1. 新增 `src/lib/i18n/`:`zh.ts`/`en.ts` 字典、`I18nProvider` + `useT()` hook(轻量自建,不引第三方);
2. `settings/store.ts` 新增 `locale` 偏好(沿用 KEY_* + writePref 模式),SettingsApp 新增语言下拉;
3. 核心文案迁移:侧边栏/右键菜单/设置窗口/状态栏/新建终端菜单/命令面板/通用按钮;
4. 验证切换即时生效 + 持久化;提交。

### Phase E — 设置入口优化 + 全量验收

1. 设置窗口 tab 重组(通用含语言/编辑器/文件树与工作区/外观/快捷键/关于),状态栏或侧边栏常驻设置齿轮(Header 已有 settingsButton,确认可用);
2. 全量验收:`pnpm lint`、`pnpm check-types`、`pnpm test`、`cargo clippy`、`pnpm tauri build`(打包注意:updater 已移除,无签名问题);
3. 同步更新 `TERAX.md`、`docs/二开规划.md`(移除已完内容)、README(裁剪/新功能说明);
4. 最终提交。

---

## 七、风险与注意事项(承接原规划)

1. **删除 ai/agents 目录前**:先用 `rg -l 'modules/ai|modules/agents' src` 确认无残留引用(见「五」),否则 tsc 大面积报错。
2. **AiComposerProvider 已整体移除**(非条件化改动),无 TERAX.md 警告的 remount 风险。
3. **Rust 命令裁剪**:lib.rs 已开 `removeUnusedCommands: true`,前端不再 invoke 的命令可安全移除;删前用 `rg 'invoke\("(git_|agent_|secrets_|ai_|lm_)' src` 确认无调用方。
4. **block 模式**(terminal blocks/editor-like input):与 ai 无关,属终端功能,**默认保留**;若 BlockOverlay 引 ai 则需剥离或删除。
5. **spaces/多根**:roots 为全局偏好,与 spaces 解耦;如后续要 per-space roots,在 useSpaces 中挂 roots 数组即可。
6. **编译资源**:已通过 `.cargo/config.toml`(jobs=6)控制;不要在 16GB 机器上同时跑 cargo + vite build。
7. **测试同步**:删除模块时对应 `*.test.ts` 必须同步删/改(已有 serialize.test/commands.test 先例)。
8. **settings store 的 model 字段**:`DEFAULT_PREFERENCES` 与 `loadPreferences` 中大量 model/provider 字段(sttProvider、lmstudio/ollama/mlx/openaiCompatible 等),清理时保持 KEY_/映射/类型三处一致;注意 `PREFERENCE_KEYS` 映射表也要删对应行,否则残留键写入报类型错。

---

## 八、交接检查单(接手后 30 分钟内可验证)

```
1. git status → 7 个未提交文件(tsc 已验证全绿)
2. pnpm check-types → 无 error
3. pnpm test → 676 通过(应有)
4. cargo check --locked → 无 error(updater/control 裁剪后)
5. pnpm tauri dev → 应用可启动,右侧终端面板 + 顶部文件 tab + 文件树盘符条正常
6. 文件树:盘符条可见(Windows)、可添加多根、右键文件夹可"Add to Workspace Roots"/"Open in Terminal"
```
