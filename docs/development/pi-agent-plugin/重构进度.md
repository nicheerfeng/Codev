# Pi Agent 重构交付记录（2026-09-10）

本轮以此文档覆盖旧执行规划中的 UI 决策。

## 起点与范围

- 现状提交：d65e16e；GitHub 备份分支 backup/pi-agent-before-rebuild-20260910、tag pi-agent-before-rebuild-20260910 已推送。
- 工作分支：feat/pi-agent-ui-rebuild。
- Pi 继续位于右侧 Dock 插件页，保留 Codev 主体结构、主题与字体；无 Electron、Claude SDK、新 Web 服务。
- mcode：参考 LeftBar、SidebarShared、ComposerEditor/Toolbar、ChatPane 的组件职责和交互，使用 Codev Button/Input/Textarea/ContextMenu/Popover 改写。
- Zeno：参考 timeline.ts 的逐 turn 聚合与 SessionTimelineScroller 的用户翻阅/自动跟随分离。
- 分组、归档只保存 Codev 展示元数据，Pi 原生 JSONL 和 models.json 保持单一数据源。

## 执行与验收

- [x] 推送旧版备份分支和 tag。
- [x] 按内容块保留正文、思考、工具顺序；一次 turn 一个过程聚合。
- [x] 线程运行状态独立，切插件和线程不终止后台任务，监听正确释放。
- [x] 左侧组/项目/归档，新增、折叠、重命名、归档恢复、线程右键。
- [x] 固定输入区、模型/思考/token、发送/停止、中文组词、图片附件。
- [x] 顶部搜索/导出/设置；直接校验保存 models.json。
- [x] 类型、协议单测、隔离后台浏览器核验，真实 RPC 非付费命令检查。
- [x] 10 并发构建便携包；记录哈希与限制，不启动用户应用。

## 验证边界

浏览器用隔离 RPC fixture 验证真实 React 组件的布局和交互，不接用户模型、不发送付费消息；原生通道另做独立验证。仅有 mock 通过不能宣称真实模型端到端通过。

## 已执行验证

- TypeScript 无错误；前端 66 文件、377 项通过，最后搜索调整后相关 23 项再次通过。
- Rust release Pi 模块 4 项测试全部通过；新的独立 target 解决旧目录编译缓存失效。
- 本机 Pi 只读 RPC：get_state/get_messages/get_available_models/get_available_thinking_levels/get_session_stats 全部成功。使用 --no-session --no-extensions --no-skills；没有模型请求、没有用户配置写入。
- 后台 Chrome：空项目添加、输入卡片可编辑、模型切换保留草稿、插件切换保留草稿和进程、40 轮原生形状历史恢复、右键重命名非当前目标、归档、设置 JSON 校验与 mock 保存。
- 滚动：主动上滚后追加 200 段文本，scrollTop 12869 → 12869；回到最新和停止按钮可用。
- 搜索：11 个可见高亮、1 个当前高亮，window.getSelection() 为空；虚拟列表只挂载 7 个块。
- 正式 React 编译器最终核验：命中文字中心偏差 -0.5px，15 个可见高亮；输入框继续编辑不会触发搜索跳转。流式、切插件、返回草稿和后台输出均通过，页面异常及 console.error 为 0。
- 尺寸监听：移除重复 ResizeObserver；使用虚拟列表的逐帧测量并关闭生命周期内 flushSync，消除两类开发控制台告警。
- 450×340 窄窗浅色：输入框可编辑且完整位于视口，无横向溢出；另核验 1000×700 深色布局。
- 未启动 Codev 或便携 EXE。真实模型生成、用户自装扩展最终由用户审核；TUI custom() 等不属于 RPC 可移植 UI。

## 本轮工程边界

- 原生读写与命令继续走 Rust；组/归档仅保存在 codev-plugins.json。
- models.json 保存后新启动会话加载，运行中任务不被强制重启。
- 没有新增 npm 依赖；由于目录搬迁恢复过现有锁文件依赖。
- Dock 到 ToolPanel 增加 active 参数；隐藏时只积累线程数据，恢复可见时同步一次，禁用 Pi 插件时卸载并释放进程。未改动主布局。
- Tailwind 只扫描 src，Vite 只预扫描产品 HTML 入口，防止下载的参考项目参与产品样式/依赖扫描。
- 复用与改写来源许可见 src/modules/plugins/pi-agent/UPSTREAM-NOTICES.md，随审核包附带。
- build-review.py 使用 tqdm 显示 3 阶段进度，CARGO_BUILD_JOBS=10，产物从独立 target 复制；不执行应用。

## 重复验证入口

```powershell
node node_modules/vite/bin/vite.js --config docs/development/pi-agent-plugin/qa.vite.config.ts
# 后台浏览器访问 http://127.0.0.1:1422/docs/development/pi-agent-plugin/qa.html
node node_modules/vitest/vitest.mjs run --exclude '.reference-cache/**' --exclude 'docs/ref-piagent/**' --maxWorkers=5
python docs/development/pi-agent-plugin/build-review.py
```

QA 入口只在本地开发服务器使用，不进入正式构建。浏览器截图保存在 output/playwright/pi-ui-final-verified.png 等文件。

## r7 审核交付

- 产品代码提交：5e25b13（feat/pi-agent-ui-rebuild），已推 GitHub；main 未合并。
- 文件：artifacts/Codev-PiAgent-UI-r7/Codev-0.8.11-PiAgent-UI-r7-Windows-x64-Portable.exe
- 大小：7,791,104 bytes（约 7.43 MiB）。
- SHA-256：B3B1436B5A694D06B56E3434A8D5BC1547A45D3635C83C2804678419E16E9937
- 未启动该 EXE；后台 Chrome 与核验开发服务器均已关闭。
