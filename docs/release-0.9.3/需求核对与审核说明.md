# Codev 0.9.3 需求核对与审核说明

状态：准备审核，未提交、未打 tag、未推送 main、未创建 GitHub Release。

## 来源
- HTML 用户消息：4 条，见 HTML用户需求.md / HTML用户需求.jsonl。
- 本次对话：8 轮，见 本次对话需求.md。
- 对外文稿：../Codev-0.9.3-发布说明.md。
- 本次对话第 2 轮引用上文定位结论的部分以摘要保留；其他任务正文按对话录入。图片只记录任务含义，不复制图片中的配置或凭据。

## 需求与最终实现
| 来源 | 需求 | 状态及代码依据 |
|---|---|---|
| HTML 1、本次 1 | 了解项目与架构 | 已完成调研，不作为版本功能 |
| HTML 2–3 | 非对话操作不启动 runtime | client.ts：hydrateFromDisk / setModel / setThinkingLevel / rename / branch；Rust 原生会话读写 |
| HTML 2–3 | 最近 20 条、上翻渐进加载 | client.ts loadOlderHistory；PiTranscript.tsx 位置保持；Rust parse_session_history |
| HTML 4、本次 2 | 回收后发送保持对话可见 | client.ts start 按缓存决定 loadingHistory，并保留 running |
| 本次 3、6 | 禁用原生菜单，提供阅读区剪贴板菜单 | lib.rs WebView2 设置；ReaderContextMenu.tsx；HTML bridge |
| 本次 3 | 切换按钮避开滚动条 | MarkdownViewToggle.tsx right-6 |
| 本次 3 | 蓝点外旋转环 | PiSidebar.tsx / pi-agent.css |
| 本次 3 | 服务商 name 同步 | modelDashboard.ts / PiSettings.tsx |
| 本次 3、5 | 气泡靠右、自动收缩、最大 80% | PiTranscript.tsx w-fit max-w-[80%]；第 5 轮更正覆盖先前固定宽度 |
| 本次 4 | 队列退回编辑、删除、立即引导 | client.ts updateQueuedMessage / PiComposer.tsx / PiAgentPane.tsx |
| 本次 5–6 | 清理超过 30 分钟的系统 runtime | 仅调研，用户暂缓；本版不实现、不承诺；原自动回收阈值仍为 20 分钟 |
| 本次 7 | 按右键标题关闭右侧全部标签并保存 | useTabs.ts scopeIds；useTabCloseGuards.ts saveDirtyTabs；跨空间回归覆盖 |
| 本次 8 | 双包、文稿、历史便携包清理 | 本轮准备，发布须用户审核后另行执行 |

## 审核边界
- 对外更新说明仅陈述功能变化，不包含校验值、包大小或内部实现细节。
- 单次真实模型调用的端到端验证尚未执行；此前 Chrome 核验使用隔离 fixture。
- 立即引导使用 Pi steer，在下一个模型可接收节点处理，不打断当前同步工具执行。
- 当前队列操作依赖 Pi clear_queue 返回的文本重建；图片附件不在该 RPC 返回值内，附件队列的无损编辑尚不能保证。此项作为审核限制报告，不在本轮擅自扩展修改。
- 本次打包包含此前已存在的未提交工作树改动，需结合待提交差异审核；没有回滚相邻模块修改。

## 本轮检查记录
- 前端：69 个测试文件、424 项测试通过。
- Rust：Pi 原生模块 6 项测试通过。
- lint：0 error、82 warning；修正本版 ReaderContextMenu 的 alert 角色使用错误，其余警告未批量修改。
- cargo fmt --check：未通过，存在跨模块格式差异；未执行全仓格式化。
- git diff --check：通过。
- 版本：package.json、Tauri 配置、Cargo package/workspace 与 Cargo.lock 均为 0.9.3。
- 旧便携包：0.9.1、0.9.2 移入回收站；历史安装包保留。清理记录位于新版本 artifacts 目录。
- 双包：由既有 build-release.py 构建，10 并发、tqdm 进度；完成后核对实际产物。
- 发布动作：本轮不提交、不推送、不打 tag、不创建 GitHub Release。
