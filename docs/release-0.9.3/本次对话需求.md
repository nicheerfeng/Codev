# 本次对话需求原文

按用户消息顺序整理，共 8 轮；保留任务正文，图片以对应需求说明标注。项目级 AGENTS.md 约束属于执行规范，不计作产品功能需求。

## 1 · 阅读项目现状
你好，感知下这个项目，，
这个项目近期在开发一个插件来试图加入一个pi-agent的插件来辅助开发；
然后这是最近的 工作进展记录D:/software/codev-dev/docs/codev-pi-agent工作进展.md
这是可以参考 相关文件：D:/software/codev-dev/docs/ref-piagent
你可以读取下D:/software/codev-dev/docs/Pi-session-pi-agent最新处理进展.html这个最后 100-200行来感知下处理进展
我需要你阅读和感知目前的开发现状，，然后向我汇报的对项目的基本情况认知，

## 2 · runtime 恢复时的消息重载
用户引用已定位的现象：空闲回收后再次发送会出现消息重载；当前回收阈值 20 分钟，start() 设置 loadingHistory，触发对话加载遮罩。
修复这个bug，无需做过度的测试，修复完基本确定ok向我汇报即可，，

## 3 · 阅读器、运行指示、服务商命名与消息布局
1、阅读html，右键会唤醒html级别的刷新进而导致 页面被强制刷新-需要禁用html阅读界面的右键触发原生浏览器的相关功能；我实测所有阅读界面的都会触发右键的和这个htm页面原生功能，都需要禁掉；
2、阅读md文档的render/raw组件盖住了右侧的进度条导致有点影响用户体验，需要稍微往左一点点；
3、pi-agent的进度感知，改为在现有的蓝色光点外侧加一层滚动环，方便用户更明确的感知进度
5、pi-agent的新增量无法使用name同步，甚至没有配置name参数（参图）；
6、pi-agent的输入对话，默认宽度，而不是自动根据文本长度伸缩跨度，且应该改为在右侧分布

图片说明：公共配置 JSON 的 name 与服务商标题、顶部服务商标签不同步。消息布局以第 5 轮更正为准。

## 4 · 排队消息操作
排队消息队列，需要支持可退回编辑、删除、以及点击就即时插入消息队列等功能，做微调补充上这些功能，如果不知道怎么做，可以去D:softwarecodev-devdocsef-piagent 参考，，

## 5 · 气泡宽度更正；runtime 清理仅调研
你先需要修一个你错误理解的问题：
用户消息统一为内容区 80% 宽度、靠右显示，不随文字长度伸缩。---错误的；
用户消息统一为内容区 80% 宽度、靠右显示，自动随文字长度伸缩展示气泡，，正确的，做微调修复即可，无需测试！

关键任务需求-调研后反馈，无需修正：
需要在pi-agent插件中设置按钮旁边增加一个一键clean-pi-agent的runtime的功能，将超过30分钟未使用系统runtime一键清理掉，，目前的回收机制有时候无法回收我在其他地方意外触发启动的runtime，， 你来思考下是否可实现，如何实现，先给我一个解决方案，我评估后请你来做执行，，

## 6 · Codev 阅读区右键剪贴板菜单
关于runtime管理的问题，容我思考下下一轮给你反馈再具体决定如何改；
你先处理有一个新的 bug，我需要主界面的阅读窗口（raw/render模式下，或json/jsonl之类的文件中）右键选中之后，增加一个适配codev风格的剪切、复制、粘贴的右键功能，，我习惯了使用快捷键来做，但暴露这个通用功能对普通用户来说还是有必要的，，你来补充即可，少做测试，，

## 7 · 关闭所选标题右侧全部标签并保存
再核验和补充一个功能：在主窗口界面中这个一键关闭右侧的按钮功能，并没有按照预期生效，现在还是只会关闭最近右侧，，我记得之前尝试修复过，但实际并没有成功，你来定位思考并修复，，让其真正的实现，以所选中header-title为基础定位一键关闭该header右侧的所有title（不包括自身，并确保会自动保存关闭的文件的更新-如有的话）。。你直接来定位修复吧，少做复杂测试，做完向我汇报即可，，

## 8 · 0.9.3 发布准备，审核后再发布
OK，你来基于D:/software/codev-dev/docs/Pi-session-pi-agent最新处理进展.html 抽取出所有人类需求对话，已经在本次历史对话中的所有需求，帮我总结一个更新摘要，，我们准备推送这个版本作为0.9.3来发布，，你需要先把文稿准备好，和两个安装文件（清理历史便携包，新建一个安装包和便携包）准备好，我来审核修正后请你发布到github主分支，，，，
